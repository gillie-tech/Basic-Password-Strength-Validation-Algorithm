from setuptools import setup, find_packages

setup(
    name='password_validator',
    version='1.0.0',
    description='A standalone heuristic-based password strength algorithm.',
    packages=find_packages(),
)
